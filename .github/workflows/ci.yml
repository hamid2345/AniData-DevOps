
name: CI/CD Pipeline

on:
  push:
    branches: [main, feature/*]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install ruff pytest requests beautifulsoup4
          pip install -r requirements.txt
      
      - name: Lint with ruff
        run: ruff check src/
      
      - name: Test with pytest
        run: pytest tests/ -v